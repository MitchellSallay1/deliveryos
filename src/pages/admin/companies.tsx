import { AdminCompanyDirectoryPage } from '@/pages/admin/company-directory'

export function AdminCompaniesPage() {
  return (
    <AdminCompanyDirectoryPage
      title="Companies"
      description="All tenant workspaces — server-side pagination"
    />
  )
}
