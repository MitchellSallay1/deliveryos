-- DeliveryOS development seed (NEVER run automatically in production)
-- Usage: psql $DATABASE_URL -f supabase/seed.sql

BEGIN;

-- Demo companies are created via RPC in local dev scripts; this file documents intended fixtures:
-- 1 logistics provider, 1 merchant, 1 hybrid, sample zones, optional marketplace request.

COMMIT;
